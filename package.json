{
  "name": "grapefruit",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec",
    "test:watch": "node --test --watch --test-reporter=spec"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}

